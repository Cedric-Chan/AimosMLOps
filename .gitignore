node_modules/
dist/
.DS_Store
*.local
local-dist/
*.visual-check.*

# 生产参考代码（内部专有，绝不可入库/推送）——————
# 本仓库是 PUBLIC，误提交内部代码会立刻公开且无法真正撤回。
# 该目录正常位于本仓库之外（工作区同级）；此处为防误复制的兜底闸门。
# 说明见工作区级 REFERENCE-REPOS.md。
aimos-model*/
*production reference*/

# 内部评审 / 实测文档（仅本地留存，禁止发布）——————
# 含线上真实库表名、配置名、实测数据量，仅用于内部方案 review。
# 与线上真实系统对应的分析文档一律不要提交，发布版请另写脱敏版。
docs/**/_internal/
docs/feature-store/design/widetable-join-performance-review.md
