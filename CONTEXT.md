# CONTEXT.md — 领域词汇表

> 本文件是 Aimos MLOps 仓库的唯一词汇表（single-context）。只放领域术语的定义与边界，不放实现细节。
> 建立于 2026-09-22 的 Evals Context 设计对齐会话；术语随讨论结晶即时追加。

## 评测（Evals）域

**评测集（Evaluation Dataset）**
一组稳定 id 的用例的集合，是质量的「测试数据库」。≠ 生产日志：生产日志是原始事实，评测集是经过人工采纳的事实子集。

**用例（Case / Dataset Item）**
一条可重复判定的输入 + 期望输出（ground truth）。≠ trace：用例可以来自 trace 回灌，也可以手工构造（无 trace）。用例 id 稳定且跨评测运行可 join。

**评测运行（Eval Run）**
对某个评测集的一次执行，记录「评测集版本 × 被测服务版本 × 评分器版本」三元组，保证可复现。

**门禁（Quality Gate）**
发布前对回归结果的一种消费方式：确定性断言不通过则阻塞发布。≠ 告警：告警是发布后对质量信号的被动通知，门禁是发布前的主动阻塞。

**业务判据（Business Criteria）**
定义「什么是对的」的标准，由业务方拥有并签署：审核标准、抽取标准、最终结论对/错的真值定义。业务方**不**定义性能指标的目标值。

**工程判据（Engineering Criteria）**
定义「做到多好」的可测指标与目标值，由工程在成本约束下校准：关键字段抽取准确率、单请求 token 上限、轮数上限、延迟上限。准确率目标不归业务定（业务会要求不可能的 100%）。

**Judge 健康度（Judge Agreement）**
LLM judge 与人工标注在同名反馈上的一致率，是 evaluator 自身的质量元指标。跌破阈值（建议 90%）的 judge 失去阻塞发布的资格，降级为只报告。

**失败转用例（Failure → Case）**
生产失败经人工采纳（人工闸门，不自动）转化为回归用例的动作。采纳不等于修复，也不等于批准发布。

## 责任层（Responsibility）

**发布指针（Production Pointer）**
指向当前生产版本的可变标签；发布权 = 移动该指针的写权限。回滚 = 把指针移回上一个通过门禁的版本。版本对象本身不可变。

**降级标记（Served Model Marker）**
一次调用中「请求的模型」与「实际服务的模型」的成对记录。缺了它，降级期间的质量指标不可归因。

## 经营层（Business Adoption）

**评测覆盖率（Eval Coverage）**
有评测集覆盖的在跑 LLM 服务数 ÷ 全部在跑 LLM 服务数。平台团队的产出所有权指标之一。

**回归拦截数（Regression Interceptions）**
门禁拦下的带病发布次数。证明门禁不是摆设的产出所有权指标之二。

**业务结果（Business Outcome）**
KYC 通过率、单据解析准确率等场景级结果。平台只提供测量能力（口径 + 趋势），**达标由业务方负责**，不进平台 OKR。

**服务接入协议（Onboarding Protocol）**
新 LLM 服务上线的准入约定：自带 ≥N 条评测用例 + 一份已签署的《通过标准》+ 具名业务签署人。把评测集冷启动成本前置到接入时刻，替代事后的集中补录。
